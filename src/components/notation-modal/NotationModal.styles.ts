import styled from '@emotion/styled';

export const styles = {
  container: styled.div`
    display: flex;
    flex-direction: column;
    color: #ededed;
    background: #222;
    justify-content: center;
    align-items: center;
    z-index: 30;
  `,
};
